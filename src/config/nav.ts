/**
 * Navigation items for the header.
 * Add/remove/reorder items here — the navbar updates automatically.
 */
export const NAV_ITEMS = [
  { label: "Posts", path: "/posts" },
  { label: "Projects", path: "/projects" },
  { label: "About", path: "/about" },
] as const;
